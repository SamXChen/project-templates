{
 "name": "test-app",
 "version": "0.1.0",
 "license": "MIT",
 "scripts": {
  "boot": "lerna bootstrap && lerna link",
  "plus": "lerna add --no-bootstrap",
  "clean": "lerna clean",
  "dev": "lerna run --parallel dev",
  "build": "lerna run build",
  "start-server": "cd ./modules/server && npm run start"
 },
 "dependencies": {
  "lerna": "3.22.1",
  "yarn": "^1.22.5"
 }
}
